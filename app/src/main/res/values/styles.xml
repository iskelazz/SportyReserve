<resources>

    <style name="LoginButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:backgroundTint">?attr/colorPrimary</item>
    </style>
    <style name="ShapeAppearance.MyApp.Button.Rounded" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Other attributes -->
        <item name="colorOnSurface">@color/colorOnSurface</item>
    </style>

    <!-- Dark theme -->
    <style name="AppTheme.Dark" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Other attributes -->
        <item name="colorOnSurface">@color/colorOnSurfaceInverse</item>
    </style>
</resources>